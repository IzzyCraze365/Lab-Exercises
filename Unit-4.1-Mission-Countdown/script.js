// Your code goes here

// Go Fishing, get the IDs from the HTML linked up
let startTimeout = getElementById(start-timeout);
let timeoutDisplay = getElementById(timeout-display);
let startInterval = getElementById(start-interval);
let intervalDisplay = getElementById(interval-display);

// Event Listeners
startTimeout.addEventListener("click", functionNameHere);
startInterval.addEventListener("click", functionNameHere);
